all:
  children:
    local:
      hosts:
        localhost:
          anisble_host: "$(hostname -i)"
          ansible_connection: "local"
          ansible_user: "root"
          ansible_port: "22"
          # ssh_private_key_file is not required as server connection is local
          # ansible_ssh_private_key_file: "~/.ssh/root"
